# waterui-video

Video components for WaterUI.

This crate provides:

- `Video`: raw video surface view for custom playback UI.
- `VideoPlayer`: video player view intended for standard playback UX.
- Shared playback types like `AspectRatio`, `Volume`, and `Event`.
