/// A recorded drawing shown as a static image.
pub mod picture;
/// The engine-owned resources content records against.
pub mod resources;
/// `WaterUI` view wrapper for scene rendering.
pub mod scene_view;
