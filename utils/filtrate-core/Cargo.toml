[package]
name = "filtrate-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.87"
readme = "../../README.md"
repository = "https://github.com/water-rs/waterui"
description = "Core traits for GPU filter pipelines - lazy, zero-copy, fuseable"
keywords = ["gpu", "filter", "wgpu", "pipeline", "compute"]
categories = ["graphics", "rendering"]
license = "MIT OR Apache-2.0"

[lints]
workspace = true

[dependencies]
wgpu.workspace = true
nami.workspace = true
