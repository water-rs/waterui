# Guide

A small markdown file so components.md's `include_markdown!("guide.md")` snippet
resolves a real path.

WaterUI renders this at compile time as a `RichText`.

- Bullets render as list items.
- *Emphasis* and **strong** are supported.
