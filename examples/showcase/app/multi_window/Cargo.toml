[package]
name = "multi-window-example"
version = "0.1.0"
edition = "2024"
authors = ["Lexo Liu"]
description = "Multi-window example demonstrating different window styles and backgrounds for WaterUI"
license.workspace = true
repository.workspace = true
readme = "../../README.md"
keywords = ["ui", "framework", "example", "window", "multi-window"]
categories = ["gui"]
publish = false

[features]
dev = ["waterui/dynamic_linking"]

[dependencies]
waterui.workspace = true
