[package]
name = "markdown-example"
version = "0.1.0"
edition = "2024"
authors = ["Lexo Liu"]
description = "Markdown example for WaterUI framework"
license.workspace = true
repository.workspace = true
readme = "../../README.md"
keywords = ["ui", "framework", "example", "markdown"]
categories = ["gui"]
publish = false

[features]
dev = ["waterui/dynamic_linking"]

[dependencies]
waterui = { workspace = true}
